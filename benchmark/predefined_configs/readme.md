# Benchmarks result

This package contains several folders each containing one or more benchmark configs regarding the subject of the folder's name it resides in.

The configs are run by `run.py` script, which will iterate over the folders and look for the config files and run them with the `orchestrator` script.

## Results

The results added to this repo are run locally on an idling machine with following specs:

Processor: Intel Core i5-5300U CPU @ 2.30GHz × 4  
Ram: 12GiB SODIMM DDR3 Synchronous 1600 MHz (4 + 8GiB)  
GPU: Intel HD Graphics 5500 (Broadwell GT2)  
Drive: 256GB TOSHIBA THNSFJ25  